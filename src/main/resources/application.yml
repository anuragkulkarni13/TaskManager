spring:
  application:
    name: TaskManager
  datasource:
    url: jdbc:mysql://localhost:3306/tmdb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true